---
import Description from '@components/work/description.astro';
import ImgLarge from '@components/work/imgLarge.astro';
import Logo from '@components/work/logo.astro';
import Share from '@components/work/share.astro';
import Title from '@components/work/title.astro';
import { projectsData } from '@data/projects';
import WorkLayout from '@layouts/WorkLayout.astro';

export function getStaticPaths() {
  return [
    { params: { project: 'gifter' } },
    { params: { project: 'commenteer' } },
    { params: { project: 'backnotes' } },
    { params: { project: 'backnotesapi' } },
  ];
}

const { project } = Astro.params;

const data = projectsData[project!];
---

<WorkLayout title="Bartek Mazur - Front-end Developer">
  <Logo />
  <Title data={data} />
  <Share data={data} />
  <ImgLarge />
  <Description data={data} />
  <!-- Description with Key Features-->
  <!-- Demo -->
  <!-- Agile board -->
  <!-- License -->
  <!-- Design -->
  <!-- Picture of app -->
  <!-- Picture of trello -->
  <!-- Technologies -->
  <!-- Acknowledgements -->
  <!-- Images -->
</WorkLayout>
